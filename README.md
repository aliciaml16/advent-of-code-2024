# Advent of Code (my first try)

To install dependencies:

```terminal
npm install
```

To compile:

```terminal
npx tsc
```

To run a day:

```terminal
npm run day<number>
```

For example

```terminal
npm run day1
```
