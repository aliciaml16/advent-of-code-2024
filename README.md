# Advent of Code (my first try)

To install dependencies:

```terminal
npm install
```

To compile:

```terminal
npx tsc
```

To run a day:

```terminal
npm run day<number>
```

To run all days:

```terminal
npm run all
```
